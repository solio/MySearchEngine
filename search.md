# 你是一个AI 搜索MCP开发专家，负责开发一个基于AI的搜索MCP，用于搜索股票、新闻、论坛等。
现在我的另一个项目researcher（在你的同级目录prompt-engineering/research.md）用的是tavily的搜索API，它有以下几个局限
* 需要付费，免费额度只有1000次/月
* 它只做AI友好的内容返回优化，并没有深入地按投研的思路去搜索关键信息，导致有一堆无效信息

# 你的TODO （未完成）
未完成则需要只做todo部分，其他部分先忽略
* 请帮我选型能在macbook或者windows wsl可以运行的、AI友好的搜索服务，类似于tavily
* 请帮我选型设计一个模型的思路（不限于llm，也可以是机器学习），可以在每次搜索后记录精品站点（或者url模式），后续加强搜索结果的筛选，例如很多站点会搜出来个股/行业的日k页面：“隆基绿能(601012)的实时行情，及时准确的提供隆基绿能(601012)的flash分时走势、K线图、均价线系统、MACD、KDJ、交易量等全面技术分析，帮你做出及时判断，同时提供隆基绿...”，完全就是垃圾信息
* 我已经有了初步优质站点了，我希望搜索引擎能加强定向的搜索，至少tavily是可以做到的

# 基本要求
* 每次生成代码都列出思考过程到一个单独的文件，文件名格式 **{日期}-主要更新内容.md**，存在thoughts/目录下
* 每次搜索的结果都输出到一个单独的文件，文件名格式 **{日期时间}-搜索结果.md**，存在search_results/**日期**目录下
* 投研的数据是与时间高度敏感相关的，请优先查找最新的热点消息，如果由于时间过滤掉一些数据，导致数据不足，则该翻页的需要翻页查找，直到找到足够的数据
* 将本python程序运行的所有日志打印在logs目录下，处理常见的网络连接错误请打印堆栈，处理常见的http请求异常请打印堆栈，日志文件 search-engine-{日期}.log（每天自动rolling）
* searxng的配置搜索选项仅使用谷歌搜索，使用**本地网络代理**，searxng使用docker-compose启动（保存配置文件），searxng的日志重定向到logs目录下,日志级别为info,searxng-{日期}.log（每天自动rolling）

# 能力迭代
* 添加读取文件的选项，根据文件里的股票列表批量投研，文件格式为**股票代码|股票名称**，每行一个股票
* 支持按行业进行搜索，批量搜索里也可以按行业+按股票来搜索，格式和个股区分开来
* 搜索失败请记录日志，也请分个类哪些方便打印在控制台的信息也输出出来，如果是未找到有效结果，请给出原因是为什么找不到，是被过滤完了，还是今天的消息就没有，还是其他原因
* 请正常添加代码相关的日志，以及打开info级别日志，正常记录第三方库的日志

# skill化
* 将本工程做成可以供其他agent调用的skill,skill单独弄个子目录
* 本工程生成的数据search_results提供给统计目录的prompt_engineer使用
* 能力：根据[某年某月某日, 个股或行业]的数据查询请求，返回相对应的数据
* 要求：skill.md中需要详细说明每个字段的用途和含义

# 代码管理
* 保持代码可读性强（适合初学者学习）
* 创建git仓库，用于管理代码，每次迭代都提交到git仓库中
* 你的格式为 **{日期时间}-{主要更新内容}**，列出top5简要修改点
* logs/pycache/.env/.claude 不要提交
* github仓库地址：https://github.com/solio/MySearchEngine , 账号godsolio@hotmail.com,密码wG36308458

# 搜索调优迭代一
* 辅助工具：**本地网络代理**端口为127.0.0.1:10795，mac的代理显示是socks代理，请用这个代理做git提交
* 在分析过程中输出评估的主要参数，是否偏离，是否需要更新模型参数，识别了几条问题、哪些问题，带--debug选项的时候可以直接将详细问题写入到搜索结果中